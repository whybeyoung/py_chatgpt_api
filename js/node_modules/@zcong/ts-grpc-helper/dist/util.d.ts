import { Metadata } from '@grpc/grpc-js';
export declare const setDeadlineHeader: (md: Metadata, deadline: number | Date) => void;
export declare const setTimeoutHeader: (md: Metadata, timeoutMs: number) => void;
